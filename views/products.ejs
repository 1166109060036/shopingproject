<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>CyloCity | เลือกสินค้า</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #f0f0f0, #d6d6d6);
      color: #333;
      font-family: "Prompt", sans-serif;
    }

    /* Navbar */
    .navbar {
      background: rgba(123, 0, 0, 0.805) !important;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }
    .navbar-nav .nav-link {
      color: white !important;
      font-weight: 500;
    }
    .navbar-nav .nav-link:hover {
      color: #ffe6e6 !important;
    }

    /* โลโก้ */
    .navbar-brand img {
      height: 40px;
    }

    /* ปุ่ม Logout */
    #logout-button {
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.6);
      border-radius: 12px;
      backdrop-filter: blur(8px);
    }
    #logout-button:hover {
      background: rgba(255, 255, 255, 0.8);
      color: #a00000;
    }

    /* Banner */
    .banner img {
      filter: brightness(0.65);
    }
    .banner h1, .banner p {
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
      background: rgba(0, 0, 0, 0.3);
      border-radius: 16px;
      padding: 10px 20px;
    }

    /* Section Heading */
    #product-section h2 {
      font-weight: 700;
      margin-bottom: 30px;
      color: #a00000;
      text-align: center;
      background: rgba(255, 255, 255, 0.4);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      display: inline-block;
      padding: 10px 30px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }

    /* Card */
    .card {
      border: none;
      border-radius: 20px;
      background: rgba(255, 255, 255, 0.45);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      box-shadow: 0 8px 24px rgba(0,0,0,0.15);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .card:hover {
      transform: translateY(-6px) scale(1.02);
      box-shadow: 0 12px 32px rgba(0,0,0,0.2);
    }
    .card-img-top {
      border-top-left-radius: 20px;
      border-top-right-radius: 20px;
      height: 250px;
      object-fit: cover;
    }

    /* ปุ่ม */
    .btn-outline-primary {
      color: #a00000;
      border-color: #a00000;
      border-radius: 12px;
      backdrop-filter: blur(6px);
    }
    .btn-outline-primary:hover {
      background: rgba(160, 0, 0, 0.8);
      color: white;
    }

    .btn-secondary {
      border-radius: 12px;
      background: rgba(150,150,150,0.5);
      color: white;
      backdrop-filter: blur(6px);
      border: none;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg p-3 fixed-top">
    <div class="container position-relative">
      <button class="navbar-toggler text-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a class="nav-link active" href="/products">สินค้า</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">ข้อมูลส่วนตัว</a></li>
          <li class="nav-item"><a class="nav-link" href="/carts">ตะกร้าสินค้า</a></li>
          <li class="nav-item"><a class="nav-link" href="/myorders">คำสั่งซื้อของฉัน</a></li>
          <li class="nav-item" style="display:none;" id="dashboard-link"><a class="nav-link" href="/dashboard">Dashboard</a></li>
        </ul>

        <div class="d-flex align-items-center">
          <span class="text-white me-3" id="user-email">Loading...</span>
          <a href="/logout" class="btn" id="logout-button">
            Logout
          </a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Banner -->
  <div class="banner position-relative" style="margin-top: 70px;">
    <img src="https://cdn.outsideonline.com/wp-content/uploads/2020/11/19/cyclist-lens-flare_h.jpg" 
        class="w-100" style="object-fit: cover; height: 800px;" alt="Banner">
    <div class="position-absolute top-50 start-50 translate-middle text-center text-white">
      <h1 class="fw-bold display-4">CyloCity</h1>
      <p class="lead">ศูนย์รวมจักรยานและอุปกรณ์ครบวงจร</p>
      <a href="#product-section" class="btn btn-light btn-lg mt-3 rounded-pill px-4 shadow">ดูสินค้าของเรา</a>
    </div>
  </div>

  <!-- Product Section -->
  <div id="product-section" class="container mt-5 pt-5 text-center">
    <h2>เลือกสินค้าของคุณ</h2>

    <div class="row g-4 mt-4">
      <% products.forEach(product => { %>
        <div class="col-md-4 col-sm-6">
          <div class="card h-100">
            <% if (product.image) { %>
              <img src="<%= product.image %>" class="card-img-top" alt="Product Image">
            <% } else { %>
              <div class="card-img-top d-flex justify-content-center align-items-center bg-light text-muted" style="height: 250px;">
                ไม่มีรูป
              </div>
            <% } %>

            <div class="card-body d-flex flex-column">
              <h5 class="card-title fw-bold"><%= product.product_name %></h5>
              <p class="card-text text-muted"><%= product.description %></p>
              <p class="text-danger fw-bold mb-1">฿ <%= product.price %></p>
              <p class="<%= product.quantity > 0 ? 'text-success' : 'text-danger' %> mb-3">
                <%= product.quantity > 0 ? 'จำนวน: ' + product.quantity : 'หมดสต็อก' %>
              </p>
              <% if (product.quantity > 0) { %>
                <a href="/products/<%= product.product_id %>" class="btn btn-outline-primary mt-auto">ดูรายละเอียด</a>
              <% } else { %>
                <button class="btn btn-secondary mt-auto" disabled>หมดสต็อก</button>
              <% } %>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const userId = localStorage.getItem('userId');
      const userRole = localStorage.getItem('userRole');

      if (userRole === 'admin') {
        document.getElementById('dashboard-link').style.display = 'inline';
      }

      if (!userId) {
        document.getElementById('user-email').textContent = "ไม่พบผู้ใช้";
        return;
      }

      fetch('/product/get-user-email', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ userId })
      })
      .then(res => res.json())
      .then(data => {
        document.getElementById('user-email').textContent = data.email || "ไม่พบอีเมล";
      })
      .catch(() => {
        document.getElementById('user-email').textContent = "โหลดอีเมลไม่สำเร็จ";
      });
    });
  </script>
</body>
</html>
