<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    body {
      background: linear-gradient(135deg, #f0f0f0, #d6d6d6);
      font-family: "Prompt", sans-serif;
      padding-top: 70px;
      color: #333;
    }

    .navbar {
      background: rgba(123, 0, 0, 0.85) !important;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
    }
    .navbar-nav .nav-link {
      color: white !important;
      font-weight: 500;
    }
    .navbar-nav .nav-link:hover {
      color: #ffe6e6 !important;
    }

    #logout-button {
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.6);
      border-radius: 12px;
      padding: 5px 12px;
    }
    #logout-button:hover {
      background: rgba(255, 255, 255, 0.8);
      color: #a00000;
    }

    h1.section-title {
      font-weight: 700;
      margin-bottom: 20px;
      color: #a00000;
      text-align: center;
      background: rgba(255, 255, 255, 0.4);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      display: inline-block;
      padding: 10px 30px;
    }

    .table thead { background: #a00000; color: white; }
    .table { border-radius: 15px; overflow: hidden; }

    .btn { border-radius: 12px; }
    .btn-danger, .btn-success {
      background: #a00000; border: none;
    }
    .btn-danger:hover, .btn-success:hover { background: #cc0000; }

    @media (max-width: 768px) {
      .table thead { display: none; }
      .table, .table tbody, .table tr, .table td { display: block; width: 100%; }
      .table tr {
        margin-bottom: 1rem;
        background: white;
        border-radius: 12px;
        padding: 10px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      }
      .table td {
        text-align: left;
        padding-left: 50%;
        position: relative;
      }
      .table td::before {
        content: attr(data-label);
        position: absolute;
        left: 15px;
        font-weight: bold;
        color: #a00000;
      }
    }
  </style>
</head>
<body>
  <!-- ✅ Navbar -->
  <nav class="navbar navbar-expand-lg fixed-top p-3">
    <div class="container-fluid">
      <a class="navbar-brand text-white fw-bold" href="/products">CyloCity</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a class="nav-link" href="/products">สินค้า</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">ข้อมูลส่วนตัว</a></li>
          <li class="nav-item"><a class="nav-link active" href="/carts">ตะกร้าสินค้า</a></li>
          <li class="nav-item"><a class="nav-link" href="/myorders">คำสั่งซื้อของฉัน</a></li>
          <li class="nav-item d-none" id="dashboard-link"><a class="nav-link" href="/dashboard">Dashboard</a></li>
        </ul>

        <div class="d-flex align-items-center">
          <span class="text-white me-3" id="user-email">Loading...</span>
          <a href="/logout" class="btn" id="logout-button">Logout</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- ✅ Cart -->
  <div class="container mt-4">
    <h1 class="section-title"><%= title %></h1>

    <% if (!cartItems || cartItems.length === 0) { %>
      <div class="alert alert-info text-center fs-5">🛒 ตะกร้าว่างเปล่า</div>
      <div class="text-center">
        <a href="/products" class="btn btn-danger btn-lg">กลับไปเลือกซื้อสินค้า</a>
      </div>
    <% } else { %>
      <form id="checkoutForm" action="/checkout" method="POST" enctype="multipart/form-data">
        <div class="table-responsive">
          <table class="table table-hover text-center align-middle">
            <thead>
              <tr>
                <th>เลือก</th>
                <th>รูป</th>
                <th>สินค้า</th>
                <th>จำนวน</th>
                <th>ราคา/หน่วย</th>
                <th>ราคารวม</th>
                <th>ลบ</th>
              </tr>
            </thead>
            <tbody>
              <% cartItems.forEach(item => { 
                  const priceNum = Number(item.price);
                  const itemTotal = priceNum * item.cart_quantity; 
              %>
              <tr>
                <td data-label="เลือก">
                  <input type="checkbox" 
                    class="item-checkbox" 
                    data-price="<%= (Number(item.price) * item.cart_quantity).toFixed(2) %>"
                    data-quantity="<%= item.cart_quantity %>"
                    data-product-id="<%= item.product_id %>"
                    value="<%= item.cart_id %>" />
                </td>
                <td data-label="รูป">
                  <% if (item.image) { %>
                    <img src="<%= item.image %>" alt="<%= item.product_name %>" class="img-fluid rounded shadow-sm" style="max-height: 80px;" />
                  <% } else { %>
                    <span class="text-muted">ไม่มีรูป</span>
                  <% } %>
                </td>
                <td data-label="สินค้า"><%= item.product_name %></td>
                <td data-label="จำนวน"><%= item.cart_quantity %></td>
                <td data-label="ราคา/หน่วย" class="text-danger fw-bold">฿ <%= priceNum.toFixed(2) %></td>
                <td data-label="ราคารวม" class="fw-bold">฿ <%= itemTotal.toFixed(2) %></td>
                <td data-label="จัดการ">
                  <button type="button" class="btn btn-sm btn-danger w-100" onclick="deleteCart('<%= item.cart_id %>')">ลบ</button>
                </td>
              </tr>
              <% }) %>
            </tbody>
            <tfoot>
              <tr>
                <th colspan="5" class="text-end">💰 ราคารวมสินค้าที่เลือก</th>
                <th colspan="2"><span id="totalSelectedPrice">0.00</span> บาท</th>
              </tr>
            </tfoot>
          </table>
        </div>

        <!-- ✅ Payment Section -->
        <div id="paymentSection" class="card mt-4" style="display: none;">
          <div class="card-body">
            <h5 class="mb-3 text-danger">
              ยอดที่ต้องชำระ: <strong><span id="ppAmountText">0.00</span> บาท</strong>
            </h5>

            <!-- 🔘 เลือกวิธีการชำระเงิน -->
            <div class="mb-3">
              <label class="form-label fw-bold">เลือกวิธีการชำระเงิน:</label>
              <div class="d-flex gap-3">
                <div class="form-check">
                  <input class="form-check-input" type="radio" name="paymentMethod" id="methodPromptPay" value="promptpay" checked>
                  <label class="form-check-label" for="methodPromptPay">QR PromptPay</label>
                </div>
                <div class="form-check">
                  <input class="form-check-input" type="radio" name="paymentMethod" id="methodBank" value="bank">
                  <label class="form-check-label" for="methodBank">โอนผ่านบัญชีธนาคาร</label>
                </div>
              </div>
            </div>

            <!-- 🧾 QR PromptPay -->
            <div id="promptpaySection">
              <div class="text-center mb-3">
                <p class="small text-muted mb-1">สแกน QR นี้เพื่อโอนเงินด้วยแอปธนาคาร</p>
                <img id="ppqr" src="" alt="PromptPay QR" class="img-fluid rounded shadow" style="max-width: 280px;">
                <div class="mt-2">
                  <small class="text-muted">PromptPay ID:
                    <strong><%= process.env.PROMPTPAY_ID || '064-975-8806' %></strong>
                  </small>
                </div>
              </div>

              <!-- 📎 แนบสลิป PromptPay -->
              <div class="border rounded p-3 bg-light mb-3">
                <label for="promptpaySlip" class="form-label">📎 แนบสลิปการชำระเงิน (PromptPay)</label>
                <input type="file" name="paymentSlip" id="promptpaySlip" class="form-control" accept="image/*">
              </div>
            </div>

            <!-- 🏦 โอนผ่านบัญชีธนาคาร -->
            <div id="bankSection" style="display: none;">
              <div class="border rounded p-3 bg-light mb-3">
                <h6 class="mb-2">บัญชีโอนเงิน: <strong>211-076-2691 ธนาคารกรุงไทย</strong></h6>
                <label for="bankSlip" class="form-label">📎 แนบสลิปการชำระเงิน (โอนผ่านบัญชี)</label>
                <input type="file" name="paymentSlip" id="bankSlip" class="form-control" accept="image/*">
              </div>
            </div>

            <input type="hidden" name="amountSelected" id="amountSelected" value="0.00">
            <button type="submit" class="btn btn-success w-100 mt-2">✅ ยืนยันการชำระเงิน</button>
          </div>
        </div>

      </form>
    <% } %>
  </div>

  <!-- ✅ Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const checkboxes = document.querySelectorAll('.item-checkbox');
    const form = document.getElementById('checkoutForm');
    const ppqr = document.getElementById('ppqr');
    const bankSection = document.getElementById('bankSection');
    const promptpaySection = document.getElementById('promptpaySection');
    const promptpaySlip = document.getElementById('promptpaySlip');
    const bankSlip = document.getElementById('bankSlip');

    // 🔄 สลับแสดงรูปแบบชำระเงิน
    document.querySelectorAll('input[name="paymentMethod"]').forEach(radio => {
      radio.addEventListener('change', (e) => {
        if (e.target.value === 'promptpay') {
          promptpaySection.style.display = 'block';
          bankSection.style.display = 'none';
          promptpaySlip.required = true;
          bankSlip.required = false;
        } else {
          promptpaySection.style.display = 'none';
          bankSection.style.display = 'block';
          promptpaySlip.required = false;
          bankSlip.required = true;
        }
      });
    });

    // 🧾 สร้าง hidden input
    function createHiddenInput(name, value) {
      const input = document.createElement('input');
      input.type = 'hidden';
      input.name = name;
      input.value = value;
      input.classList.add('dynamic-hidden');
      return input;
    }

    // 🗑️ ลบสินค้าแบบ AJAX
    async function deleteCart(cartId) {
      if (!confirm('คุณแน่ใจหรือว่าต้องการลบสินค้านี้?')) return;
      const res = await fetch(`/carts/delete/${cartId}`, { method: 'POST' });
      if (res.ok) location.reload();
      else alert('ลบสินค้าไม่สำเร็จ');
    }

    // 💰 คำนวณยอดรวมและอัปเดต QR
    function togglePaymentSectionAndTotal() {
      let total = 0, anyChecked = false;
      document.querySelectorAll('.dynamic-hidden').forEach(e => e.remove());

      checkboxes.forEach(cb => {
        if (cb.checked) {
          anyChecked = true;
          const price = parseFloat(cb.dataset.price);
          total += price;
          form.appendChild(createHiddenInput('cart_ids', cb.value));
          form.appendChild(createHiddenInput('quantities', cb.dataset.quantity));
          form.appendChild(createHiddenInput('product_ids', cb.dataset.productId));
          form.appendChild(createHiddenInput('prices', price));
        }
      });

      const section = document.getElementById('paymentSection');
      const totalText = document.getElementById('totalSelectedPrice');
      const amountText = document.getElementById('ppAmountText');
      const amountHidden = document.getElementById('amountSelected');

      section.style.display = anyChecked ? 'block' : 'none';
      totalText.textContent = total.toFixed(2);
      amountText.textContent = total.toFixed(2);
      amountHidden.value = total.toFixed(2);

      if (ppqr) {
        if (anyChecked) {
          const amt = encodeURIComponent(total.toFixed(2));
          ppqr.src = `/payment/pp-qr?amount=${amt}`;
          ppqr.alt = `PromptPay QR ${amt} บาท`;
        } else {
          ppqr.removeAttribute('src');
        }
      }
    }

    // 🛡️ ป้องกันชำระเงินโดยไม่เลือกสินค้า
    form.addEventListener('submit', (e) => {
      const checked = [...checkboxes].some(cb => cb.checked);
      if (!checked) {
        e.preventDefault();
        alert('กรุณาเลือกสินค้าก่อนชำระเงิน');
      }
    });

    checkboxes.forEach(cb => cb.addEventListener('change', togglePaymentSectionAndTotal));
  </script>


  <!-- ✅ โหลดอีเมล -->
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const userId = localStorage.getItem('userId');
      const userRole = localStorage.getItem('userRole');

      if (userRole === 'admin')
        document.getElementById('dashboard-link').classList.remove("d-none");

      if (!userId) {
        document.getElementById('user-email').textContent = "ไม่พบผู้ใช้";
        return;
      }

      fetch('/product/get-user-email', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ userId })
      })
      .then(res => res.json())
      .then(data => {
        document.getElementById('user-email').textContent = data.email || "ไม่พบอีเมล";
      })
      .catch(() => {
        document.getElementById('user-email').textContent = "โหลดอีเมลไม่สำเร็จ";
      });
    });
  </script>
</body>
</html>
