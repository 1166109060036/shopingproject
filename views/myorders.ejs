<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>คำสั่งซื้อของฉัน</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #f0f0f0, #d6d6d6);
      font-family: "Prompt", sans-serif;
      padding-top: 70px;
    }

    /* Navbar */
    .navbar {
      background: rgba(123, 0, 0, 0.85) !important;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
    }
    .navbar-nav .nav-link {
      color: white !important;
      font-weight: 500;
    }
    .navbar-nav .nav-link:hover {
      color: #ffe6e6 !important;
    }
    .navbar-toggler-icon {
      filter: invert(1);
    }

    /* Logout */
    #logout-button {
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.6);
      border-radius: 12px;
      padding: 5px 12px;
    }
    #logout-button:hover {
      background: rgba(255, 255, 255, 0.8);
      color: #a00000;
    }

    /* Section Title */
    h2.section-title {
      font-weight: 700;
      margin-bottom: 30px;
      color: #a00000;
      text-align: center;
      background: rgba(255, 255, 255, 0.4);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      display: inline-block;
      padding: 10px 30px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }

    /* Table */
    .table thead {
      background: #a00000;
      color: white;
    }
    .table {
      border-radius: 15px;
      overflow: hidden;
    }

    /* ปุ่มดาวน์โหลด PDF */
    .btn-pdf {
      color: #a00000;
      font-weight: 500;
      text-decoration: none;
    }
    .btn-pdf:hover {
      color: #cc0000;
      text-decoration: underline;
    }

    /* Responsive Table → Card View */
    @media (max-width: 768px) {
      .table thead {
        display: none;
      }
      .table, .table tbody, .table tr, .table td {
        display: block;
        width: 100%;
      }
      .table tr {
        margin-bottom: 1rem;
        background: white;
        border-radius: 12px;
        padding: 12px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      }
      .table td {
        text-align: left;
        padding-left: 50%;
        position: relative;
      }
      .table td::before {
        content: attr(data-label);
        position: absolute;
        left: 15px;
        font-weight: bold;
        color: #a00000;
      }
      .table img {
        max-width: 100%;
        height: auto;
      }
    }
  </style>
</head>
<body>
  <!-- ✅ Navbar -->
  <nav class="navbar navbar-expand-lg fixed-top p-3">
    <div class="container-fluid">
      <a class="navbar-brand text-white fw-bold" href="/products">CyloCity</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a class="nav-link" href="/products">สินค้า</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">ข้อมูลส่วนตัว</a></li>
          <li class="nav-item"><a class="nav-link" href="/carts">ตะกร้าสินค้า</a></li>
          <li class="nav-item"><a class="nav-link active" href="/myorders">คำสั่งซื้อของฉัน</a></li>
          <li class="nav-item d-none" id="dashboard-link"><a class="nav-link" href="/dashboard">Dashboard</a></li>
        </ul>

        <div class="d-flex align-items-center">
          <span class="text-white me-3" id="user-email">Loading...</span>
          <a href="/logout" class="btn" id="logout-button">Logout</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- ✅ My Orders Section -->
  <div class="container mt-4">
    <h2 class="section-title">คำสั่งซื้อของฉัน</h2>

    <div class="table-responsive">
      <table class="table table-hover text-center align-middle">
        <thead>
          <tr>
            <th>รูป</th>
            <th>ชื่อสินค้า</th>
            <th>จำนวน</th>
            <th>ราคา</th>
            <th>สถานะ</th>
            <th>วัน/เวลา ที่สั่ง</th>
            <th>พิมพ์ใบเสร็จ</th>
          </tr>
        </thead>
        <tbody>
          <% orders.forEach(order => { %>
          <tr>
            <td data-label="รูป">
              <% if (order.image) { %>
                <img src="<%= order.image %>" width="80" class="img-thumbnail rounded shadow-sm">
              <% } else { %>
                <span class="text-muted">ไม่มีรูป</span>
              <% } %>
            </td>
            <td data-label="สินค้า"><%= order.product_name %></td>
            <td data-label="จำนวน"><%= order.quantity %></td>
            <td data-label="ราคา" class="text-danger fw-bold">฿ <%= order.price %></td>
            <td data-label="สถานะ"><span class="badge bg-secondary"><%= order.status %></span></td>
            <td data-label="วันที่"><%= new Date(order.created_at).toLocaleString() %></td>
            <td data-label="ใบเสร็จ">
              <a href="/invoice/<%= order.order_id %>" class="btn-pdf">
                Download <i class="bi bi-file-pdf"></i>
              </a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const userId = localStorage.getItem('userId');
      const userRole = localStorage.getItem('userRole');
      if (userRole === 'admin') document.getElementById('dashboard-link').classList.remove("d-none");

      if (!userId) {
        document.getElementById('user-email').textContent = "ไม่พบผู้ใช้";
        return;
      }

      fetch('/product/get-user-email', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ userId })
      })
      .then(res => res.json())
      .then(data => {
        document.getElementById('user-email').textContent = data.email || "ไม่พบอีเมล";
      })
      .catch(() => {
        document.getElementById('user-email').textContent = "โหลดอีเมลไม่สำเร็จ";
      });
    });
  </script>
</body>
</html>
